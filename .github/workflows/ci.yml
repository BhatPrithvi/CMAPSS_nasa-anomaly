name : CI Pipeline (Docker)

on :
  push : 
    branches : 
      - feature_ci_cd_github_actions
  pull_request :
    branches : 
      - feature_ci_cd_github_actions

jobs :
  Docker:
    runs-on : ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name : Set up python
      uses : actions/setup-python@v4
      with:
        python-version : '3.9'

    - name : Install dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install -r requirements.txt

    - name : Test Holder
      run : echo "No tests yet"

    - name : Build Docker image
      run: docker build -t nasa-anomaly:latest .


    - name : Install Kind
      run : |
        curl -Lo ./kind https://kind.sigs.k8s.io/dl/v0.20.0/kind-linux-amd64
        chmod +x ./kind
        sudo mv ./kind /usr/local/bin/kind
        
    - name : Install kubectl
      run : |
        curl -Lo "https://di.k8s.io/release/$(curl -L -s https://dl.ks.io/release/stable.txt)/bin/linux/amd64/kubectl"
        chmod +x kubectl
        sudo mv kubectl /use/local/bin/

    - name : create Kind cluster
      run : kind create cluster --name nasa-anomaly

    - name : Load Docker image into Kind
      run : kind load docker-image nasa-anomaly:latest .

    - name : Deploy Flask app
      run : |
        kubectl apply -f k8s/deployment.yaml
        kubectl rollout status deployment/cmapss-isolationforest
        kubectl get pods
        kubectl get svc

    - name : Smoke test via ClusterIP + curl pod 
      run : |
        # Run a temporary curl pod to test service
        kubectl run curl-test --rm -i --restart= Never \
        --image=curlimages/curl \
        -- curl -s http://CmapssClusterIP-service :8000/ || exit 1
